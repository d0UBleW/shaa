---
actions:
  chpasswd:
    title: "Change password if expired"
    vars:
      new_passwd:
        default:
        description: |
          New password to replace expired password
        value_type: sensitive
        valid:
